# 致爱语言系统检查与修复计划

经过对"致爱"编程语言源代码（包括解释器、解析器和虚拟机）的全面检查与测试，发现了以下逻辑与架构缺陷。

---

## 1. 虚拟机（VM）变量查找顺序错误（严重）
**文件**：`zhiai/vm.py` 第 226-233 行
**问题描述**：`LOAD` 指令优先在 `globals` 中查找变量，其次才在 `locals` 中查找。这导致**函数内部无法正确遮蔽同名全局变量**——函数参数和局部变量会被全局变量"穿透"。
**测试现象**：
```
让 x = "全局"
函数 测试(x)
  输出(x)    // 期望 "参数"，实际输出 "全局"
结束
测试("参数")
```
**说明**：`STORE` 指令（第 235-241 行）的查找顺序是正确的（优先 locals），只有 `LOAD` 需要修复。

## 2. 解释器（Interpreter）循环体共享作用域（设计约束，暂不修复）
**文件**：`zhiai/interpreter.py` 第 138-145 行（WhileStmt）、第 147-171 行（ForStmt）
**问题描述**：`当`/`循环` 的循环体直接在当前 `Environment` 中执行。循环体内用 `让` 声明的变量会写入同一作用域。
**影响**：在实践中，循环变量通常用 `i = i + 1`（赋值，非 `让`）更新，不会出问题。但若在循环体内用 `让` 重新声明同名变量会覆盖循环变量。
**为何暂不修复**：尝试为循环体创建子 Environment 后，自举编译器（`compiler/main.za`）内部大量依赖循环体共享作用域的模式，子作用域会导致编译器变量绑定失败。这是一个需要在语言层面统一定义的设计决策（函数作用域 vs 块级作用域），不宜在当前阶段单独修改。

## 3. 复合赋值（+=, -=）的重复求值副作用（中等）
**文件**：`zhiai/parser.py` 第 97-102 行
**问题描述**：复合赋值 `a[取()] += 1` 被展开为 `a[取()] = a[取()] + 1`。左侧的目标表达式会被求值两次——一次用于读取原值，一次用于确定赋值目标。如果目标包含函数调用或有副作用的表达式，会导致重复执行。
**代码**：
```python
# parser.py 第 101-102 行
return ast.ExprStmt(ast.Assign(expr, ast.BinaryOp(expr, op_map[op_tok.type], value)))
#                                              ^^^^  expr 被引用了两次
```

## 4. 虚拟机不支持块级作用域（架构限制）
**文件**：`zhiai/vm.py` 整体架构
**问题描述**：VM 的调用栈帧（Frame）仅有 `locals` 一层字典，没有作用域链。所有局部变量共享同一层。这意味着：
- 如果修复了解释器的块级作用域，解释模式和编译模式的行为会不一致
- `当`/`循环` 内的 `让` 变量在 VM 中也是扁平的（但因编译器用 `STORE`/`DEF` 写入同一帧，实际行为与解释器类似）

**影响**：这是致爱语言当前的设计约束，不是回归 bug。如需支持块级作用域，需要给 VM 加 scope chain 或改用嵌套 Environment。

## 5. CLI pause() 阻塞管道（次要）
**文件**：`zhiai_cli.py`
**问题描述**：`run`、`compile`、`exec` 等命令执行完后调用 `pause()` 等待回车。这对双击执行友好，但会阻塞命令行管道和自动化脚本。
**建议**：检测是否在交互终端（`sys.stdin.isatty()`），非交互模式下跳过 pause。

---

## 已修复的问题

### VM LOAD 变量查找顺序（已修复）
**文件**：`zhiai/vm.py` 第 226-233 行
**修复**：将 LOAD 指令的查找顺序从 `globals → locals` 改为 `locals → globals`。

### 复合赋值重复求值（已修复）
**文件**：`zhiai/parser.py` 第 97-102 行，`zhiai/ast_nodes.py`，`zhiai/interpreter.py`
**修复**：新增 `CompoundAssign` AST 节点，解释器新增 `eval_compound_assign` 方法，目标表达式只求值一次。

### CLI pause 阻塞管道（已修复）
**文件**：`zhiai_cli.py`
**修复**：`pause()` 新增 `sys.stdin.isatty()` 检查，非交互终端下跳过等待。

### VM STORE_INDEX 弹栈顺序错误（已修复）
**文件**：`zhiai/vm.py` 第 270-273 行
**问题**：编译器按 `[value, obj, index]` 压栈（index 在栈顶），但 VM 按 `value, index, obj` 弹栈，变量名错位导致冒泡排序等场景崩溃。
**修复**：弹栈顺序改为 `index, obj, value`。

### VM 不支持闭包（已修复）
**文件**：`zhiai/vm.py` — `MAKE_FUNC` 和 `CALL` 指令
**问题**：VM 创建函数时未捕获外层作用域变量，导致嵌套函数无法访问外层变量（闭包失效）。解释器模式正常，但编译后的字节码模式下闭包完全不工作。
**修复**：
- `MAKE_FUNC`：创建函数时捕获当前帧的 `locals` 和 `globals` 快照存入 `closure` 字段
- `CALL`：调用用户函数时，先注入闭包变量到新帧，再绑定参数（参数优先级高于闭包）
**验证**：`test_closure.za` 输出 `加五(10) = 15`，解释器和 VM 模式均正确

---

## Open Questions

> **关于作用域行为的设计决策**：
> 解释器和 VM 在作用域模型上存在偏差。修复前需要统一语言规范。
>
> **选项 A：函数作用域**（类似旧版 JS `var`）
> 保持现有架构，只修复 VM 的 LOAD 查找顺序。循环内的 `让` 变量自然可见于外层。简单，但行为可能违反直觉。
>
> **选项 B：块级作用域**（类似 Python / ES6 `let`）
> 解释器需要为 `if`/`while`/`for` 的 body 创建子 Environment。VM 需要加 scope chain（嵌套 Frame 或 scope 指令），编译器也需要相应修改。工作量大，但更现代。
>
> **建议**：先选 A（修复最小化），后续有需求再升级到 B。

---

## Proposed Changes

### [已完成] 优先级 1：修复 VM 变量查找
`zhiai/vm.py` — LOAD 指令查找顺序已改为 `locals → globals`。

### [暂不修复] 优先级 2：解释器循环体作用域
尝试为循环体创建子 Environment 后，自举编译器内部依赖共享作用域的模式会失败。需要在语言设计层面统一后再处理。

### [已完成] 优先级 3：修复复合赋值重复求值
`zhiai/parser.py` — 新增 `CompoundAssign` AST 节点，`zhiai/interpreter.py` 新增 `eval_compound_assign` 方法。目标只求值一次。

### [已完成] 优先级 4：CLI pause 智能化
`zhiai_cli.py` — `pause()` 新增 `sys.stdin.isatty()` 检查，管道模式下自动跳过。

---

## Verification Plan
1. ~~编写 `test_scope.za`：函数内局部变量遮蔽全局变量，验证 LOAD 修复~~ ✓ 已验证（`python -m zhiai -e "让 x='全局'; 函数 测试(x); 输出(x); 结束; 测试('局部')"` → 输出"局部"）
2. 块级作用域测试暂跳过（依赖语言设计决策）
3. ~~运行 `my_demo.za`（冒泡排序）、`demo.za`、已有的 `.zab` 测试文件，确认无回归~~ ✓ 已验证
4. ~~测试管道模式不被 pause 阻塞~~ ✓ 已验证（`sys.stdin.isatty()` 检查）
