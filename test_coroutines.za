// 验证 v1.1.0 协程原语 (异步/等待)
输出("=== 协程原语验证 ===")

异步 函数 获取数据(id)
    输出("开始获取 ID: " + 转换文字(id))
    // 模拟耗时操作 (目前 VM 中等待是阻塞的，但语义已建立)
    返回 {"id": id, "内容": "致爱核心数据"}
结束

异步 函数 演示协程()
    输出("1. 准备调用异步函数...")
    让 任务 = 异步 获取数据(101) // 显式异步调用，返回 Future
    输出("2. 已拿到任务凭证，继续执行其他逻辑...")
    
    让 结果 = 等待 任务 // 阻塞等待结果
    输出("3. 任务完成，结果：" + 结果["内容"])
结束

演示协程()
输出("=== 验证结束 ===")
