// 验证延迟执行、JIT 热点加速、GC 回收以及多态内联缓存 (PIC)
输出("=== 综合高级特性验证 ===")

// 1. 验证延迟执行 (Defer)
函数 演示延迟()
    让 文件 = "测试.txt"
    输出("1. 打开文件: " + 文件)
    延迟 函数()
        输出("3. 延迟任务执行：关闭文件 " + 文件)
    结束
    输出("2. 正在读取内容...")
结束
演示延迟()

// 2. 验证多态内联缓存 (PIC)
类 鸭子
    函数 叫() 返回 "嘎嘎" 结束
结束
类 猫
    函数 叫() 返回 "喵喵" 结束
结束
类 狗
    函数 叫() 返回 "汪汪" 结束
结束

函数 动物大合唱(动物)
    输出("动物叫声: " + 动物.叫())
结束

让 动物们 = [鸭子(), 猫(), 狗(), 鸭子()]
对于 每个 动物 在 动物们 中
    动物大合唱(动物) // 这里会触发 PIC 的 MONO -> POLY 转换
结束

// 3. 验证 JIT 热点加速
函数 斐波那契(n)
    如果 n < 2 则 返回 n 结束
    返回 斐波那契(n - 1) + 斐波那契(n - 2)
结束

让 t1 = 时间()
让 结果 = 斐波那契(15) // 多次调用或深度递归会触发 JIT 编译
让 t2 = 时间()
输出("JIT 验证: F(15) = " + 转换文字(结果) + "，耗时: " + 转换文字(t2 - t1) + "秒")

// 4. 验证 GC 垃圾回收
函数 创建大量对象()
    循环 i 从 1 到 500
        让 临时 = {"id": i, "数据": "很大很大很大..."}
    结束
结束

输出("开始压力测试 GC...")
创建大量对象() // 超过阈值 300 会触发 collect
输出("GC 压力测试完成")

输出("=== 验证结束 ===")
