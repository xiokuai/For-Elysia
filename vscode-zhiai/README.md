# 致爱 (Zhiai) VSCode 插件

这是为“致爱”编程语言提供官方支持的 VSCode 插件。

## 功能特性
- **语法高亮**：全面支持致爱语言关键字、常量、运算符的高亮。
- **智能补全**：提供数十个内置函数和方法的代码补全与提示。
- **代码片段**：输入 `如果`、`当`、`循环`、`函数` 快速生成代码块。
- **自动注释**：支持快捷键自动添加 `//` 或 `/* */` 注释。

## 使用方法
安装后，所有 `.za` 和 `.zab` 扩展名的文件都将自动应用此插件的功能。
