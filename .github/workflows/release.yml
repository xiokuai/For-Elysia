name: Release ZhiAi

on:
  push:
    tags:
      - 'v*' # Push events to matching v*, i.e. v1.0, v20.15.10

jobs:
  build:
    name: Build and Release
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.8'

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller cython setuptools
          python setup.py build_ext --inplace

      - name: Build zhiai.exe
        run: |
          pyinstaller zhiai_cli.py --name zhiai --onefile --icon=logo.ico --add-data "logo.ico;." --noconfirm

      - name: Build zhiai_fast.exe
        run: |
          pyinstaller zhiai_cli.py --name zhiai_fast --onefile --icon=logo.ico --add-data "logo.ico;." --hidden-import zhiai._fastvm --noconfirm

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Build VSCode Extension
        run: |
          cd vscode-zhiai
          npm install
          npx vsce package
        shell: pwsh

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/zhiai.exe
            dist/zhiai_fast.exe
            vscode-zhiai/*.vsix
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
