// 自举验证测试
函数 阶乘(数)
  如果 数 <= 1 则
    返回 1
  结束
  返回 数 * 阶乘(数 - 1)
结束

让 结果 = 阶乘(6)
输出("6! = " + 转换文字(结果))

让 数据 = [1, 2, 3, 4, 5]
让 总和 = 0
让 索引 = 0
当 索引 < 长度(数据) 时
  让 元素 = 数据[索引]
  总和 = 总和 + 元素
  索引 = 索引 + 1
结束
输出("数组求和: " + 转换文字(总和))

如果 结果 > 100 则
  输出("大数")
否则如果 结果 > 50 则
  输出("中数")
否则
  输出("小数")
结束
